import Alert from "../../components/TailwindUI/Alert";

# Server Side Rendering (SSR)

GQty handles Server Side Rendering (SSR) out of the box, with no extra
configuration needed.

## `prepareRender`

<Alert>
  GQty uses
  [`react-ssr-prepass`](https://github.com/FormidableLabs/react-ssr-prepass)
  internally for suspense fetching, liberating developers from the need to
  separate fetching from the rendering component.
</Alert>

## `hydrateCache`

### Delaying fetches for hydration
